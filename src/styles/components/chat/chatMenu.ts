import { StyleSheet } from 'react-native';

export const chatMenu = StyleSheet.create({
    header: {
        flex: 1,
        fontSize: 16,
        fontWeight: '700',
        lineHeight: 40,
    }
});

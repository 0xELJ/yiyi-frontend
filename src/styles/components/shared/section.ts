import { StyleSheet } from 'react-native';

export const section = StyleSheet.create({
    base: {
        marginBottom: 16,
        backgroundColor: '#fff',
        justifyContent: 'flex-start',
        flexDirection: 'row',
        position: 'relative'
    }
});

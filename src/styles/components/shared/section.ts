import { StyleSheet } from 'react-native';

export const section = StyleSheet.create({
    base: {
        marginBottom: 25,
        justifyContent: 'flex-start',
        flexDirection: 'row',
        position: 'relative'
    }
});

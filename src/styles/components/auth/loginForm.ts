import { StyleSheet } from 'react-native';

export const loginForm = StyleSheet.create({
    title: {
        fontSize: 20,
        textAlign: 'center',
        lineHeight: 40
    },
    error: {
        fontSize: 20,
        alignSelf: 'center',
        color: 'red'
    }
});
